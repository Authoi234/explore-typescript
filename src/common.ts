// (null, undefined, any) type, Type Assertions(as)
// Non-null Assertion Operator (Postfix!),
// Optional Chaining (?.)
// Literal Types(value as type)
// Literal Interfaces(as const)

// let language = "Typescript";

// interface User {
//     name?: string;
//     email: string;
// }

// let user:User | null | undefined = null;

// let user = {} as User;

// user = {
//     name: 'John',
//     email: 'john@example.com'
// }

// user?.email

// interface Discription {
//     arif: string;
//     sharif: string;
// }

// let discription: Discription = {} as Discription;

// const friends = ['arif', 'sharif'] as const

// const user = {
//     name: 'John',
//     email: 'john@example.com'
// } as const;

// friends.forEach(it => {
//     discription[it] = it + "on fire"
// })